name: Build APK
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Simple Build APK
        uses: pavi2410/pwa-to-apk-action@v1
        with:
          # هنا نضع رابط الموقع أو المسار، لكننا سنستخدم أداة أبسط
          url: 'https://google.com' 
          # ملاحظة: إذا لم ينجح هذا، سننتقل فوراً للخطة ب
